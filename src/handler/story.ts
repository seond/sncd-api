/**
 *
  TODO : Needs to implement functions in each model
  function getOneById(id: string, cascade: boolean = false): Promise<Object>
  function getAll(cascade: boolean = false): Promise<Object>
  without a direct dependency to the DB.
  */
